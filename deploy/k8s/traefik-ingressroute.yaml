apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: alephat
  namespace: alephat
spec:
  entryPoints:
    - websecure
  routes:
    - kind: Rule
      match: Host(`alephat.example.com`) && PathPrefix(`/hooks/`)
      priority: 100
      services:
        - name: alephat-webhook
          port: 80
    - kind: Rule
      match: Host(`alephat.example.com`)
      priority: 10
      services:
        - name: alephat-ui
          port: 80
  tls:
    certResolver: letsencrypt
